spring:
  application:
    name: ap-pilot

  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb;Mode=PostgreSQL
    platform: h2
  jpa:
    hibernate:
      ddl-auto: create